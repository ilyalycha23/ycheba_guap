-- Добавляет новый столбец "телефон" (пока допускает NULL).
ALTER TABLE сотрудник
    ADD COLUMN телефон varchar(20) NULL;

-- Меняет тип столбца "телефон" на более длинный varchar(30).
ALTER TABLE сотрудник
    ALTER COLUMN телефон TYPE varchar(30);

-- Добавляет ограничение уникальности номеров телефона.
ALTER TABLE сотрудник
    ADD CONSTRAINT сотрудник_уник_телефон_демо UNIQUE (телефон);

-- Удаляет добавленное ограничение уникальности (если оно есть).
ALTER TABLE сотрудник
    DROP CONSTRAINT IF EXISTS сотрудник_уник_телефон_демо;

-- Удаляет столбец "телефон" из таблицы.
ALTER TABLE сотрудник
    DROP COLUMN телефон;

-- Переименовывает столбец "фио" во "фио_полное".
ALTER TABLE сотрудник
    RENAME COLUMN фио TO фио_полное;

-- Возвращает исходное имя столбца: "фио_полное" обратно в "фио".
ALTER TABLE сотрудник
    RENAME COLUMN фио_полное TO фио;