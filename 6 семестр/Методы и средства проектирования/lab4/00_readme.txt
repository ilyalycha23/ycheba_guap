Лабораторная работа №4 — по **методичка.pdf**, глава **«4. Запросы на языке SQL: агрегатные функции»**. Вариант **12**. СУБД: PostgreSQL.

Нумерация в PDF (не путать с главой 1 оглавления книги):
- **п. 4.1** — агрегатные функции (count, sum, avg, min, max), **GROUP BY**, **HAVING**;
- **п. 4.2** — **UNION**, **INTERSECT**, **EXCEPT**, теоретико-множественные операции и мультимножества (**ALL** / по умолчанию устранение дубликатов);
- **п. 4.3** — **Лабораторная работа 4** (запросы **г)–е)** по варианту из приложения, дополнительные агрегаты, демонстрация **п. 4.2**).

Подготовка БД:
1) ../lab1/01_schema.sql  
2) ../lab2/01_insert_test_data.sql  
3) (по желанию) `04_extra_data_for_lr4.sql` — доп. строки, чтобы запросы **г)–е)** в `01_queries_gde.sql` не давали пустой выборки; только в папке **lab4**, **lab2 не трогает**.

Файлы:
- `01_queries_gde.sql` — **вариант 12**, пункты **г)–е)** приложения (текст задания — в PDF).
- `02_extra_aggregates.sql` — дополнительные агрегаты (**п. 4.1**), не дублирующие приём в г)–е).
- `03_set_operations_4_2.sql` — директивы **п. 4.2** + сравнение множество / мультимножество.

Отчёт (как в п. 4.3): тексты SQL с пояснениями и результаты запросов.

Примечание: **INTERSECT ALL** / **EXCEPT ALL** в конце `03_set_operations_4_2.sql` — для **PostgreSQL 15+**; в **MS SQL** для **INTERSECT**/**EXCEPT** с **ALL** действуют ограничения (см. сноску в методичке).
