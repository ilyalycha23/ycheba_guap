Лабораторная работа №5 — по методичка.pdf (глава 5, п. 5.4 «Лабораторная работа 5»). Вариант 12. СУБД: PostgreSQL.

Подготовка:
1) ../lab1/01_schema.sql  
2) ../lab2/01_insert_test_data.sql  
3) (рекомендуется) `04_extra_data_lr5.sql` — добавляет строки в **штатное_расписание**, чтобы запрос **з)** не был пустым (файл только в **lab5**, **lab2 не меняет**).  
4) (по желанию) ../lab4/04_extra_data_for_lr4.sql — если нужны те же данные, что для ЛР4.

Файлы:
- `01_queries_zhi.sql` — **ж)–и)** варианта 12 через **[NOT] EXISTS** и подзапросы.  
- `02_dml_subqueries.sql` — примеры **INSERT / UPDATE / DELETE** с подзапросами (п. 5.1), на **временных таблицах**, без порчи основных данных.  
- `03_exists_vs_set_nulls.sql` — те же идеи, что **INTERSECT / EXCEPT** в п. 4.2, через **EXISTS / NOT EXISTS**; короткий пример с **NULL** (п. 5.3).  

Отчёт: тексты SQL с пояснениями и результаты выполнения.  
Шаблон в Word: **otchet_lr5.docx** — титул, цель, **развёрнутый ход работы** по шагам, места под результаты; структура согласована с **otchet_lr5_soderzhanie.txt**. Пересборка: `python build_lab_reports_5_8_docx.py` из корня каталога дисциплины.
