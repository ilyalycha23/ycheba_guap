Лабораторная работа №5 — содержание отчёта (методичка.pdf, п. 5.4, вариант 12)

1. Цель
   Запросы с [NOT] EXISTS и подзапросами; подзапросы в DML (п. 5.1); сопоставление с теоретико-множественными операциями и NULL (п. 5.3).

2. Ход работы (текстом, по шагам)
   Подготовка: выполнить **lab1/01_schema.sql**, **lab2/01_insert_test_data.sql**; при необходимости **lab4/04_extra_data_for_lr4.sql**.  
   Для запроса **з)** — выполнить **04_extra_data_lr5.sql**.  
   По очереди выполнить и кратко описать словами: **01_queries_zhi.sql** (логика ж, з, и через EXISTS / NOT EXISTS); **02_dml_subqueries.sql** (временные таблицы, INSERT/UPDATE/DELETE с подзапросами); **03_exists_vs_set_nulls.sql** (аналоги INTERSECT/EXCEPT и сравнение IS NULL с = NULL).  
   Полный развёрнутый текст — в шаблоне **otchet_lr5.docx** (раздел 2).

3. Запросы ж)–и) варианта 12
   Текст и пояснение **01_queries_zhi.sql**; результаты выполнения.
   — **ж)** подразделения, в которых не работают совместители;  
   — **з)** подразделения, в которых представлены все должности (в скрипте — должности с ид 1–4; при необходимости скорректируйте под справочник);  
   — **и)** сотрудники, у которых все дети одного пола.  
   Для **з)** выполните перед этим **04_extra_data_lr5.sql** (добавляет недостающие строки штата для ОГК).

4. DML с подзапросами (п. 5.1)
   Листинг и результаты **02_dml_subqueries.sql** (INSERT / UPDATE / DELETE на временных таблицах).

5. EXISTS и п. 4.2, NULL (п. 5.3)
   Листинг и результаты **03_exists_vs_set_nulls.sql**: эквивалент INTERSECT/EXCEPT через EXISTS; два запроса с NULL (**IS NULL** vs **= NULL**).

6. Вывод
   Кратко: чем EXISTS удобнее/отличается от пересечения «в лоб»; зачем IS NULL.

7. Приложения
   Файлы **lab5/*.sql**, при необходимости **lab1**, **lab2**, **lab4/04_extra_data_for_lr4.sql**.
